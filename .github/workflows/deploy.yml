name: Deploy

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout      
        uses: actions/checkout@v4

      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version: 18
      
      - name: Set cache
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm run test

      - name: Build page
        run: npm run build

      #- name: Upload artifact
      # uses: actions/upload-pages-artifact@v3
      # with:
      #    path: public/ 

  # Despliega el sitio a GitHub Pages después de completar el trabajo de build.
  # - Se ejecuta en Ubuntu.
  # - Requiere permisos para escribir en Pages y para el id-token.
  # - Define el entorno 'github-pages' y utiliza la URL de la página desplegada.
  # - Usa la acción 'actions/deploy-pages@v4' para realizar el despliegue.
  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build
  #
  #   permissions:
  #     pages: write
  #     id-token: write
  #
  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deploy-pages.outputs.page_url }}
  #
  #   steps:
  #     - name: Deploy to pages
  #       id: deploy-pages
  #       uses: actions/deploy-pages@v4
  